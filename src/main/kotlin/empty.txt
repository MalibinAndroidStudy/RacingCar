//Strategy Pattern Practice
    //참고용 코드, Not Production Code
    fun strategicalCalculate(symbol: String, leftValue: Double, rightValue: Double) : Double {
        val operator = Operator.findBySymbol(symbol)
        return operator.calculateStrategy.calculate(leftValue, rightValue)
    }

    // private const val PLUS = "+"
            // private const val MINUS = "-"
            // private const val MULTIPLE = "*"
            // private const val DIVIDE = "/"